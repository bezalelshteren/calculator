from shaipe import shape


class calculator:
    def __init__(self):
        self.add_shape_to_list()
        self.shape = shape

    @staticmethod
    def calculate(self,self.shape):
        print(shape.__str__())
        return shape

    def add_shape_to_list(self,type,calculate(shape)):
        list_of_shape = {}
        if type in list_of_shape:
            list_of_shape[type] = []
            list_of_shape[type].append(shape)
        else:
            list_of_shape[type] = shape




calculator = calculator()